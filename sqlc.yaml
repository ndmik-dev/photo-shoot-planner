version: "2"

sql:
  - engine: "postgresql"
    queries: "sql/queries"
    schema: "migrations"
    gen:
      go:
        package: "dbgen"
        out: "internal/platform/dbgen"
        sql_package: "pgx/v5"